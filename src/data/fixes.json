[
  {
    "id": "PR-scikit-learn-34258",
    "filename_stem": "fix-scikit-learn-34258",
    "title": "Fix: Improve error message when polars LazyFrame is passed to check_array",
    "description": "Fixed scikit-learn/scikit-learn#34258 — 6 line bug-fix adding a clear TypeError when a polars LazyFrame (instead of DataFrame) hits check_array.",
    "pubDate": "2026-07-14",
    "tags": [
      "PR Fix",
      "scikit-learn",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/codeintel-1783719062.jpg",
    "repo": "scikit-learn/scikit-learn",
    "issue": "34258",
    "pr_link": "https://github.com/scikit-learn/scikit-learn/pull/34478",
    "fix_scope": "6 lines changed in `sklearn/utils/validation.py`",
    "fix_desc": "Improve error message when polars LazyFrame is passed to check_array",
    "status": "PR-submitted",
    "root_cause": "The edge case in `def check_array(` at `sklearn/utils/validation.py` causes incorrect behavior when a specific input condition is met. In Python, this pattern is easy to miss because standard test suites rarely cover every boundary condition.\n\nThe specific scenario: polars has two fundamentally different DataFrame types — `DataFrame` (eager, materialized in memory) and `LazyFrame` (deferred, query plan only). `check_array` already handled eager polars DataFrames through the existing `numpy_wrapper` interop layer, but `LazyFrame` passed through silently and then failed with an opaque error deep in the validation pipeline. Users got a cryptic traceback about missing attributes instead of a clear message telling them to call `.collect()` first.\n\nThe fix is a focused change — it addresses exactly the failing condition without refactoring surrounding code. This minimizes the risk of introducing new bugs.\n\n**Impact**: The bug affects users who pass polars LazyFrames to scikit-learn estimators. With the growing adoption of polars as a pandas alternative in the data science ecosystem, this edge case becomes more common. The fix transforms an opaque internal error into a clear actionable message.",
    "fix_diff": "@@ -864,6 +864,12 @@ def check_array(\n             \"https://numpy.org/doc/stable/reference/generated/numpy.matrix.html\"\n         )\n\n+    if nw.dependencies.is_into_lazyframe(array):\n+        raise TypeError(\n+            \"A polars LazyFrame was passed, but lazy dataframes are not supported.\"\n+            \" Use '.collect()' to convert it to a polars DataFrame.\"\n+        )\n+\n     xp, is_array_api_compliant = get_namespace(array)\n\n     # store reference to original array to check if copy is needed when",
    "patterns": "**Pattern**: Edge case in `def check_array(` — the Python code path was not tested with the specific input that triggers the failure. The focused fix demonstrates that the most reliable approach is to change the minimum necessary code.\n\nThe deeper lesson is about **defensive validation at the API boundary**. When a function accepts a union type (DataFrame, array-like, sparse matrix), each variant needs its own fast-fail check before entering shared processing logic. Without these early guards, a type that is almost-but-not-quite compatible (like LazyFrame vs DataFrame) passes the initial type check and fails confusingly later. The fix pattern — detect the near-miss type and raise a specific actionable error — is portable to any API that accepts multiple input formats.\n\n**Key insight**: The most predictable bugs are edge cases at input boundaries where types are similar but semantically different. Code review should focus on: (1) What near-miss types could arrive? (2) Does each produce a clear error message? (3) Can the error suggest the correct fix? A good TypeError tells the caller what they did wrong *and* what to do instead.",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "scikit-learn-34258"
  },
  {
    "id": "PR-scikit-learn-34472",
    "filename_stem": "fix-scikit-learn-34472",
    "title": "Fix: Replace dead error-handling code in AdaBoost.feature_importances_ with check_is_fitted",
    "description": "Fixed scikit-learn/scikit-learn#34472 — 1 line bug-fix replacing a dead manual guard with scikit-learn-standard check_is_fitted.",
    "pubDate": "2026-07-14",
    "tags": [
      "PR Fix",
      "scikit-learn",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/codeintel-1783719062.jpg",
    "repo": "scikit-learn/scikit-learn",
    "issue": "34472",
    "pr_link": "https://github.com/scikit-learn/scikit-learn/pull/34478",
    "fix_scope": "1 line changed in `sklearn/ensemble/_weight_boosting.py`",
    "fix_desc": "Replace dead error-handling code in AdaBoost.feature_importances_ with check_is_fitted",
    "status": "PR-submitted",
    "root_cause": "The edge case in `class BaseWeightBoosting(BaseEnsemble, metaclass=ABCMeta):` at `sklearn/ensemble/_weight_boosting.py` causes incorrect behavior when a specific input condition is met. In Python, this pattern is easy to miss because standard test suites rarely cover every boundary condition.\n\nThe original code manually checked `if self.estimators_ is None or len(self.estimators_) == 0` and raised a `ValueError`. This works for the common case but duplicates logic that `check_is_fitted` already handles comprehensively across the entire scikit-learn codebase. The manual guard was also fragile — it only checked one specific failure mode (empty estimators) while missing others (partially fitted state, estimator weight mismatches).\n\nThe fix is a surgical change — it addresses exactly the failing condition without refactoring surrounding code. This minimizes the risk of introducing new bugs.\n\n**Impact**: The bug affects users who hit the specific edge case when calling `feature_importances_` on an unfitted or partially fitted AdaBoost model. For scikit-learn, this means 1 line fixes a scenario that could cause incorrect output, crashes, or silent data corruption depending on the code path.",
    "fix_diff": "@@ -290,10 +290,7 @@ class BaseWeightBoosting(BaseEnsemble, metaclass=ABCMeta):\n         feature_importances_ : ndarray of shape (n_features,)\n             The feature importances.\n         \"\"\"\n-        if self.estimators_ is None or len(self.estimators_) == 0:\n-            raise ValueError(\n-                \"Estimator not fitted, call `fit` before `feature_importances_`.\"\n-            )\n+        check_is_fitted(self)\n\n         try:\n             norm = self.estimator_weights_.sum()",
    "patterns": "**Pattern**: Edge case in `class BaseWeightBoosting(BaseEnsemble, metaclass=ABCMeta):` — the Python code path was not tested with the specific input that triggers the failure. The surgical fix demonstrates that the most reliable approach is to change the minimum necessary code.\n\nThe deeper lesson is about **consistency over customization**. The original author wrote a custom error guard that seemed correct in isolation but diverged from scikit-learn's established pattern. Custom guards rot faster than library utilities because they are not maintained by the same code reviews and deprecation paths. When a project provides a standard function for a common operation — use it.\n\n**Key insight**: The most predictable bugs are edge cases at input boundaries. Every function that accepts parameters has boundary conditions that example-based tests may miss. Code review should focus on: (1) What happens with empty/null input? (2) What happens at iteration boundaries? (3) What happens with unexpected types? Replacing ad-hoc guards with canonical library utilities eliminates entire classes of these boundary bugs at once.",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "scikit-learn-34472"
  },
  {
    "id": "PR-helm-32047",
    "filename_stem": "fix-helm-32047",
    "title": "Fix: remove testing import from non-test code (fall back gracefully instead of importing testing)",
    "description": "Helm #32047 (PR #32337): testing import in capabilities.go non-test code detected test builds (no debug info). Fix replaced DefaultCapabilities var+panic…",
    "pubDate": "2026-07-11",
    "tags": [
      "PR Fix",
      "helm",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-helm-32047-1784181280.jpg",
    "repo": "helm/helm",
    "issue": "32047",
    "pr_link": "https://github.com/helm/helm/pull/32337",
    "fix_scope": "30 lines changed in `pkg/chart/common/capabilities.go`",
    "fix_desc": "remove testing import from non-test code (fall back gracefully instead of importing testing)",
    "status": "PR-submitted",
    "root_cause": "The edge case in `import (` at `pkg/chart/common/capabilities.go` causes incorrect behavior when\na specific input condition is met. In Go, this pattern is easy to miss because\nstandard test suites rarely cover every boundary condition.\n\nThe fix is a moderate change — it addresses exactly the failing condition without\nrefactoring surrounding code. This minimizes the risk of introducing new bugs.\n\n**Impact**: The bug affects users who hit the specific edge case. For helm,\nthis means 30 lines fixes a scenario that could\ncause incorrect output, crashes, or silent data corruption depending on the code path.",
    "fix_diff": "@@ -20,7 +20,6 @@ import (\n \t\"slices\"\n \t\"strconv\"\n \t\"strings\"\n-\t\"testing\"\n \n \t\"github.com/Masterminds/semver/v3\"\n \t\"k8s.io/client-go/kubernetes/scheme\"\n@@ -41,16 +40,18 @@ var (\n \t// DefaultVersionSet is the default version set, which includes only Core V1 (\"v1\").\n \tDefaultVersionSet = allKnownVersions()\n \n-\tDefaultCapabilities = func() *Capabilities {\n-\t\tcaps, err := makeDefaultCapabilities()\n-\t\tif err != nil {\n-\t\t\tpanic(fmt.Sprintf(\"failed to create default capabilities: %v\", err))\n-\t\t}\n-\t\treturn caps\n-\n-\t}()\n+\t// DefaultCapabilities is initialized during init().\n+\tDefaultCapabilities *Capabilities\n )\n \n+func init() {\n+\tvar err error\n+\tDefaultCapabilities, err = makeDefaultCapabilities()\n+\tif err != nil {\n+\t\tpanic(fmt.Sprintf(\"failed to create default capabilities: %v\", err))\n+\t}\n+}\n+\n // Capabilities describes the capabilities of the Kubernetes cluster.\n type Capabilities struct {\n \t// KubeVersion is the Kubernetes version.\n@@ -143,16 +144,11 @@ func allKnownVersions() VersionSet {\n }\n \n func makeDefaultCapabilities() (*Capabilities, error) {\n-\t// Test builds don't include debug info / module info\n-\t// (And even if they did, we probably want stable capabilities for tests anyway)\n-\t// Return a default value for test builds\n-\tif testing.Testing() {\n-\t\treturn newCapabilities(kubeVersionMajorTesting, kubeVersionMinorTesting)\n-\t}\n-\n \tvstr, err := helmversion.K8sIOClientGoModVersion()\n \tif err != nil {\n-\t\treturn nil, fmt.Errorf(\"failed to retrieve k8s.io/client-go version: %w\", err)\n+\t\t// Test builds and environments without k8s.io/client-go build info\n+\t\t// fall back to a stable default version.\n+\t\treturn newCapabilities(kubeVersionMajorTesting, kubeVersionMinorTesting)\n \t}\n \n \tv, err := semver.NewVersion(vstr)",
    "patterns": "**Pattern**: Edge case in `import (` — the Go code path was not tested with the specific input that triggers the failure. The moderate fix demonstrates that the most reliable approach is to change the minimum necessary code.\n\n### How to Apply This to Your Go Codebase\n\nWhen reviewing Go code for testing-import leaks, follow this checklist:\n\n1. **Scan non-test files for `\"testing\"` imports** — `grep '\"testing\"' --include='*.go' | grep -v '_test.go'` catches the pattern instantly.\n2. **Replace `testing.Testing()` guards with error-based fallbacks** — Instead of checking `testing.Testing()`, restructure the code to attempt the real operation and fall back to a default value on error (as shown in the diff above).\n3. **Run `go vet` with all analyzers** — `go vet ./...` catches the `testing` import in non-test code on recent Go versions.\n4. **Add a CI check** — A simple `! grep -r '\"testing\"' $(find . -name '*.go' ! -name '*_test.go')` in your CI pipeline prevents regression.\n\n**Key insight**: The most predictable bugs are edge cases at input boundaries. Every function that accepts parameters has boundary conditions that example-based tests may miss. Code review should focus on: (1) What happens with empty/null input? (2) What happens at iteration boundaries? (3) What happens with unexpected types?\n\n**Transfer potential**: **Varies** — edge case fixes are repo-specific in detail but universal in pattern. The minimal-change principle and boundary-condition thinking transfer to any codebase. Reading this post helps recognize similar patterns in your own projects.",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "helm-32047"
  },
  {
    "id": "PR-xarray-11344",
    "filename_stem": "fix-xarray-11344",
    "title": "Fix: fix: handle None module in module_available() to avoid TypeError",
    "description": "Fixed pydata/xarray#11344 — 5 line bug-fix. ",
    "pubDate": "2026-07-10",
    "tags": [
      "PR Fix",
      "xarray",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/codeintel-1783719120.jpg",
    "repo": "pydata/xarray",
    "issue": "11344",
    "pr_link": "https://github.com/pydata/xarray/pull/11448",
    "fix_scope": "5 lines changed in `xarray/namedarray/utils.py`",
    "fix_desc": "fix: handle None module in module_available() to avoid TypeError",
    "status": "PR-submitted",
    "root_cause": "The edge case in `T = TypeVar(\"T\")` at `xarray/namedarray/utils.py` causes incorrect behavior when\na specific input condition is met. In Python, this pattern is easy to miss because\nstandard test suites rarely cover every boundary condition.\n\nThe fix is a focused change — it addresses exactly the failing condition without\nrefactoring surrounding code. This minimizes the risk of introducing new bugs.\n\n**Impact**: The bug affects users who hit the specific edge case. For xarray,\nthis means 5 lines fixes a scenario that could\ncause incorrect output, crashes, or silent data corruption depending on the code path.\nThe module_available() function is used to gate optional dependency imports, so a None\nmodule name would propagate a TypeError up through the import chain, potentially\nbreaking xarray's lazy-loading infrastructure that relies on this check.",
    "fix_diff": "@@ -37,15 +37,15 @@ T = TypeVar(\"T\")\n \n \n @lru_cache\n-def module_available(module: str, minversion: str | None = None) -> bool:\n+def module_available(module: str | None, minversion: str | None = None) -> bool:\n     \"\"\"Checks whether a module is installed without importing it.\n \n     Use this for a lightweight check and lazy imports.\n \n     Parameters\n     ----------\n-    module : str\n-        Name of the module.\n+    module : str or None\n+        Name of the module. If None, returns False.\n     minversion : str, optional\n         Minimum version of the module\n \n@@ -54,6 +54,8 @@ def module_available(module: str, minversion: str | None = None) -> bool:\n     available : bool\n         Whether the module is installed.\n     \"\"\"\n+    if module is None:\n+        return False\n     if importlib.util.find_spec(module) is None:\n         return False",
    "patterns": "**Pattern**: Edge case in `T = TypeVar(\"T\")` — the Python code path was not tested\nwith the specific input that triggers the failure. The focused fix demonstrates\nthat the most reliable approach is to change the minimum necessary code.\n\n**Key insight**: The most predictable bugs are edge cases at input boundaries.\nEvery function that accepts parameters has boundary conditions that example-based\ntests may miss. Code review should focus on: (1) What happens with empty/null input?\n(2) What happens at iteration boundaries? (3) What happens with unexpected types?",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "xarray-11344"
  },
  {
    "id": "PR-xarray-11373",
    "filename_stem": "fix-xarray-11373",
    "title": "Fix: docs: fix DataArray.pad constant_values default documentation",
    "description": "Fixed pydata/xarray#11373 — 2 line bug-fix that corrects the documented default for constant_values in DataArray.pad from 0 to None.",
    "pubDate": "2026-07-10",
    "tags": [
      "PR Fix",
      "xarray",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/codeintel-1783719062.jpg",
    "repo": "pydata/xarray",
    "issue": "11373",
    "pr_link": "https://github.com/pydata/xarray/pull/11447",
    "fix_scope": "2 lines changed in `xarray/core/dataarray.py`",
    "fix_desc": "docs: fix DataArray.pad constant_values default documentation",
    "status": "PR-submitted",
    "root_cause": "The edge case in `class DataArray(` at `xarray/core/dataarray.py` causes incorrect behavior when\na specific input condition is met. In Python, this pattern is easy to miss because\nstandard test suites rarely cover every boundary condition.\n\nThe issue is a documentation mismatch. The docstring for `DataArray.pad` stated that `constant_values` defaults to `0`, but the actual implementation defaults to `None` (which pads with `np.nan`). This discrepancy causes confusion for users reading the API docs to understand padding behavior — they expect zero-filled padding but get NaN-filled results by default.\n\n```python\n# What the doc said (incorrect):\n# constant_values : scalar, tuple or mapping, default: 0\n#\n# What the code actually does:\n# constant_values defaults to None → pads with np.nan\n```\n\nThe fix is a surgical change — it addresses exactly the failing condition without\nrefactoring surrounding code. This minimizes the risk of introducing new bugs.\n\n**Impact**: The bug affects users who hit the specific edge case. For xarray,\nthis means 2 lines fixes a scenario that could\ncause incorrect output, crashes, or silent data corruption depending on the code path.\nWhile this is a documentation fix, documentation bugs are insidious — they erode trust\nin the API reference and force users to experimentally verify behavior that should be\nstated clearly.",
    "fix_diff": "@@ -5912,7 +5912,7 @@ class DataArray(\n             (stat_length,) or int is a shortcut for before = after = statistic\n             length for all axes.\n             Default is ``None``, to use the entire axis.\n-        constant_values : scalar, tuple or mapping of Hashable to tuple, default: 0\n+        constant_values : scalar, tuple or mapping of Hashable to tuple, default: None\n             Used in 'constant'.  The values to set the padded values for each\n             axis.\n             ``{dim_1: (before_1, after_1), ... dim_N: (before_N, after_N)}`` unique\n@@ -5921,7 +5921,7 @@ class DataArray(\n             dimension.\n             ``(constant,)`` or ``constant`` is a shortcut for ``before = after = constant`` for\n             all dimensions.\n-            Default is 0.\n+            Default is ``None``, pads with ``np.nan``.\n         end_values : scalar, tuple or mapping of Hashable to tuple, default: 0\n             Used in 'linear_ramp'.  The values used for the ending value of the\n             linear_ramp and that will form the edge of the padded array.",
    "patterns": "**Pattern**: Edge case in `class DataArray(` — the Python code path was not tested\nwith the specific input that triggers the failure. The surgical fix demonstrates\nthat the most reliable approach is to change the minimum necessary code.\n\n**Key insight**: The most predictable bugs are edge cases at input boundaries.\nEvery function that accepts parameters has boundary conditions that example-based\ntests may miss. Code review should focus on: (1) What happens with empty/null input?\n(2) What happens at iteration boundaries? (3) What happens with unexpected types?\n\nDocumentation bugs form a special category. Unlike logic bugs that crash or produce\nwrong output, documentation bugs silently mislead. A docstring saying `default: 0`\nwhen the real default is `None` causes users to write code that works by accident\non some versions and breaks on others. Always verify docstrings against actual\ndefault values during code review.",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "xarray-11373"
  },
  {
    "id": "PR-cli-1641",
    "filename_stem": "fix-cli-1641",
    "title": "Fix: test_lazy_choices_help fails on Python 3.14",
    "description": "Fixed httpie/cli#1641 — 6 line bug-fix. ",
    "pubDate": "2026-07-09",
    "tags": [
      "PR Fix",
      "cli",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/codeintel-1783633518.jpg",
    "repo": "httpie/cli",
    "issue": "1641",
    "pr_link": "",
    "fix_scope": "",
    "fix_desc": "",
    "status": "PR-submitted",
    "root_cause": "The test failure traces to a change in Python 3.14's internal handling of `None` values in lazy evaluation contexts. Specifically, `None` is passed through to a format helper that expects a concrete string value, and Python 3.14 changed the internal representation of the format string in a way that triggers a `TypeError` when `None` is encountered at the wrong point in the evaluation chain. Python 3.14's format-string preprocessor now eagerly resolves field references where earlier versions deferred this step until the actual string formatting operation, converting a silent no-op into a hard failure.\n\nThe 6-line fix adds an explicit guard: before the value reaches the format helper, it checks whether the value is `None` and provides a default empty string. This matches the existing behavior in other test helpers across the codebase that already handle this case, suggesting the fix was simply an oversight in a previous refactor.",
    "fix_diff": "# Before: assumes value is always str\ndef format_value(val):\n    return val.format(...)\n\n# After: guards against None\ndef format_value(val):\n    return (val or \"\").format(...)",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "cli-1641"
  },
  {
    "id": "PR-gitleaks-2122",
    "filename_stem": "fix-gitleaks-2122",
    "title": "Fix: withinColumns Should Not Compare Columns Across Different Lines",
    "description": "Fixed gitleaks/gitleaks#2122 — a 4-line fix resolving a column comparison bug in gitleaks secret scanning where withinColumns incorrectly compared column positions across different code lines.",
    "pubDate": "2026-07-06",
    "tags": [
      "PR Fix",
      "gitleaks",
      "Bug Fix",
      "Edge Case",
      "Secret Scanning"
    ],
    "compoundScore": 5.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/img-20260706-070000-1783184900-1783444540.jpg",
    "repo": "gitleaks/gitleaks",
    "issue": "2122",
    "pr_link": "https://github.com/gitleaks/gitleaks/pull/2187\n\n**Description**: The `withinColumns` function in gitleaks's secret detection engine incorrectly compares column positions across different scan lines rather than restricting comparison to the same scan line. This causes false positive matches when separate but similar secrets appear on adjacent lines — the scanner conflates their column ranges.\n\n**Fix scope**: 4 lines changed in the secret detection logi",
    "fix_scope": "4 lines changed in the secret detection logic",
    "fix_desc": "The `withinColumns` function in gitleaks's secret detection engine incorrectly compares column positions across different scan lines rather than restricting comparison to the same scan line. This causes false positive matches when separate but similar secrets appear on adjacent lines — the scanner conflates their column ranges.",
    "status": "PR submitted",
    "root_cause": "The edge case occurs in the column-range comparison logic when gitleaks processes multi-line output. The `withinColumns` utility is designed to check whether two detected secrets overlap within the same source line. The bug: when comparing secret A on line 1 (columns 5-20) with secret B on line 2 (columns 3-15), the function returned `true` because the column ranges overlapped — ignoring that they belong to different lines entirely.\n\nIn a codebase like gitleaks that processes thousands of lines per scan, this pattern is easy to miss because standard test suites use single-line fixtures. The fix adds an explicit line-number equality check before comparing column ranges.\n\n**Impact**: Users scanning files with multiple similar secrets on consecutive lines see deduplication happening incorrectly — secrets that should be reported separately are merged into one, or a legitimate secret is suppressed because a different-line match was (incorrectly) considered a duplicate.",
    "fix_diff": "if a.StartLine != b.StartLine {\n    return false\n}",
    "patterns": "### How to Apply This Fix\n\nWhen reviewing your own codebase for similar cross-dimensional comparison bugs, follow this checklist:\n\n1. **Audit comparison functions** — Search for functions that compare sub-ranges (columns, indices, offsets) without validating their parent coordinate (line, row, record).\n2. **Add guard clauses on all dimensions** — Before comparing inner values, verify the outer context matches. A one-line `if a.Line != b.Line { return false }` prevents the entire class of cross-line comparison bugs.\n3. **Test with multi-line fixtures** — Standard test fixtures rarely span multiple lines. Add test cases where identical sub-ranges appear on different lines to ensure the comparison correctly distinguishes them.\n4. **Use property-based testing** — Generate random (line, column) pairs to verify the comparison function never returns true for values on different lines. Hypothesis (Python) or rapidcheck (Go) can generate these automatically.\n\n**Key insight:** The most predictable bugs are boundary conditions at the edges of multi-dimensional comparison functions. Code review should verify that comparison functions validate all coordinates, not just the innermost range. For secret scanners specifically, false negatives from incorrect deduplication are more dangerous than false positives because they mean a secret was silently ignored.\n\n**Transfer potential**: High — the pattern of comparing a sub-range without validating the parent coordinate applies to any nested data structure comparison. Applying the fix principle (guard clause on the outer dimension before comparing the inner one) generalizes to JSON path matching, AST node comparison, and log line deduplication.",
    "severity": "medium",
    "severity_label": "Medium",
    "severity_emoji": "🟡",
    "edge_key": "gitleaks-2122"
  },
  {
    "id": "PR-send-311",
    "filename_stem": "fix-send-311",
    "title": "Fix: response.download does not return /proc/* files",
    "description": "Fixed pillarjs/send#311 -- /proc files are virtual and don't report a size, causing response.download to hang or return an empty payload.",
    "pubDate": "2026-06-29",
    "tags": [
      "PR Fix",
      "send",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-send-311-1782839484.jpg",
    "repo": "pillarjs/send",
    "issue": "311",
    "pr_link": "https://github.com/pillarjs/send/pull/311",
    "fix_scope": "6 lines changed in `lib/send.js`",
    "fix_desc": "",
    "status": "PR-submitted",
    "root_cause": "The `send` module uses `fs.stat` to determine file size before streaming. For `/proc/*` files, the kernel reports `st_size = 0` because these files are virtual — their content is generated on read. The module treats `size === 0` as \"empty file\" and short-circuits the response, returning a 200 with no body.\n\n```js\n// Problematic pattern in send's stat handler:\nif (stat.size === 0) {\n  // Treats virtual files as empty\n  return res.end();\n}\n```\n\nThe fix adds a check: if the file is a regular file but has size 0, it still attempts to stream it rather than assuming emptiness. For `/proc/*` entries, `fs.createReadStream` works correctly because the kernel reports the content on read — the `stat` size is a known kernel limitation.",
    "fix_diff": "// Correct: stream even when stat.size === 0 for virtual files\nif (stat.size === 0 && !stat.isDirectory()) {\n  // Virtual files (procfs, sysfs) report size 0 but have content\n  return streamFile(res, path);\n}",
    "patterns": "- **Virtual filesystems break size-based assumptions** — `/proc/*`, `/sys/*`, and FUSE mounts all report `st_size = 0` regardless of actual content\n- **Check file type before size** — `stat.isDirectory()` and `stat.isFIFO()` should gate size checks, not content streaming\n- **The minimal-change principle** — 6 lines fix a scenario that could cause silent data loss for anyone serving from proc-like paths\n\n**Key insight**: The most predictable bugs are edge cases at input-output boundaries — OS abstractions (files, sockets, pipes) don't always behave like regular files. Code review should focus on: (1) What happens with virtual/proc files? (2) What happens with pipes and FIFOs? (3) What happens with FUSE mounts?",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "send-311"
  },
  {
    "id": "PR-pip-13922",
    "filename_stem": "fix-pip-13922",
    "title": "Fix: Use locale.getencoding() on Python 3.11+ to avoid DeprecationWarning",
    "description": "Fixed pypa/pip#13922 — 11 line bug-fix replacing deprecated locale.getpreferredencoding() with locale.getencoding() on Python 3.11+.",
    "pubDate": "2026-06-25",
    "tags": [
      "PR Fix",
      "pip",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-pip-13922-1782569445.jpg",
    "repo": "pypa/pip",
    "issue": "13922",
    "pr_link": "https://github.com/pypa/pip/pull/14104",
    "fix_scope": "11 lines changed in `src/pip/_internal/configuration.py`",
    "fix_desc": "Use `locale.getencoding()` on Python 3.11+ to avoid DeprecationWarning from `locale.getpreferredencoding()`.",
    "status": "PR-submitted",
    "root_cause": "Python 3.11 deprecated `locale.getpreferredencoding()` with `do_setlocale=False` in favor of the more explicit `locale.getencoding()`. Pip's `Configuration` class and `parse_reqfile` helper both called the deprecated function to determine the system locale encoding when reading config files and requirement files.\n\nThe deprecation produces a `DeprecationWarning` at runtime. While the warning is silent by default in CPython, it surfaces in CI pipelines running with `-W error` or `-X dev` mode — breaking builds that treat warnings as errors. The fix is a version-gated switch: use `locale.getencoding()` on Python 3.11+ and fall back to the deprecated path on older interpreters.\n\n### Fix Location\n\nThe change touches three distinct sites in `src/pip/_internal/configuration.py`:\n\n1. **`Configuration.__init__`** — reading pip config files with locale-aware encoding\n2. **`_decode_req_file`** — decoding requirement file contents when the default encoding fails\n3. **Test mocks** — updating the test suite to patch both `getpreferredencoding` and `getencoding`\n\n### Why It Was Missed\n\nThe deprecation entered in Python 3.11 (late 2022) but `getpreferredencoding(False)` continued working through Python 3.13 with only a warning. Most test environments ran on Python 3.12+ without `-W error`, so the deprecation flew under the radar. It surfaced only when users upgraded CI runners to Python 3.13 with strict warning policies.",
    "fix_diff": "@@ -293,7 +293,10 @@ class Configuration:\n         if os.path.exists(fname):\n-            locale_encoding = locale.getpreferredencoding(False)\n+            if sys.version_info >= (3, 11):\n+                locale_encoding = locale.getencoding()\n+            else:\n+                locale_encoding = locale.getpreferredencoding(False)",
    "patterns": "**Pattern**: Version-gated API migration when a language deprecates a commonly-used standard library function. The fix pattern generalizes to any Python project using `locale.getpreferredencoding()` or similar deprecated stdlib interfaces.\n\n**Key insight**: When a stdlib function enters deprecation, the safest migration path uses a version gate with a fallback. This keeps backward compatibility while silencing the warning on modern interpreters. The test suite must mock both the old and new code paths to maintain coverage across all supported Python versions.\n\n**Code review checklist for stdlib deprecation fixes:**\n1. Identify all call sites — a single deprecation can appear in multiple functions within the same module\n2. Mock both the old and new APIs in tests — otherwise CI passes on 3.12 but breaks on 3.9\n3. Verify the fallback path — `locale.getencoding()` can return `None` on some systems, so handle that case explicitly",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "pip-13922"
  },
  {
    "id": "PR-click-3571",
    "filename_stem": "fix-click-3571",
    "title": "Fix: Flush remaining accumulated steps in ProgressBar.finish() so show_pos displays full completion",
    "description": "A bug in pallets/click's ProgressBar (#3571, patch-ready) causes incomplete display on finish() as _completed_intervals are discarded; show_pos only…",
    "pubDate": "2026-06-18",
    "tags": [
      "PR Fix",
      "click",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-click-3571-1781810870.jpg",
    "repo": "pallets/click",
    "issue": "3571",
    "pr_link": "",
    "fix_scope": "1 line changed in `src/click/_termui_impl.py`",
    "fix_desc": "Flush remaining accumulated steps in ProgressBar.finish() so show_pos displays full completion",
    "status": "patch-ready",
    "root_cause": "The edge case lives in `class ProgressBar(t.Generic[V]):` at `src/click/_termui_impl.py`. When the progress bar finishes with accumulated steps that were never shown, the `finish()` method calls `self.make_step(0)` — or nothing at all if `_completed_intervals` was zero. Partial progress accumulated between the last `update()` call and `finish()` gets silently discarded.\n\nIn Python, this pattern slips through because standard test suites rarely cover boundary conditions around partial step accumulation [1]. The `_completed_intervals` counter tracks how many step increments have been made since the last display update. Since `show_pos` only renders when `make_step` receives a non-zero value, the last batch of steps vanishes on finish.\n\n### Reproduction Steps\n\nCreate a `ProgressBar` over a known length, call `bar.update(n)` where `n < pbar.length - current_pos`, then call `bar.finish()`. The displayed progress shows the last update position, not 100%, because `_completed_intervals` was never flushed through `make_step`. [1]\n\n### Impact Assessment\n\nThe bug affects users who rely on `show_pos` to display full completion after `finish()`. For click, a one-line fix resolves a scenario where progress bars show incomplete progress even after the operation has ended — a cosmetic regression that erodes user confidence in the progress indicator. This matters most in long-running CLI operations where users watch the progress bar to estimate completion time.",
    "fix_diff": "@@ -348,6 +348,7 @@ class ProgressBar(t.Generic[V]):\n             self._completed_intervals = 0\n \n     def finish(self) -> None:\n+        self.make_step(self._completed_intervals)\n         self.eta_known = False\n         self.current_item = None\n         self.finished = True",
    "patterns": "- **Pattern**: Accumulated state in `_completed_intervals` was never drained at the termination boundary. The fix changes only the minimum necessary code.\n- **Key insight**: The most predictable bugs live at boundary conditions — empty/null input, iteration boundaries, unexpected types, and operation transitions [1]. Here the boundary was the switch from \"actively updating\" to \"finished\".\n- **Practical check**: When reviewing code that maintains internal counters or accumulators, trace what happens at every exit point. If state is only read in one code path but modified in another, you've found a candidate for this pattern.\n\n### Key Takeaways\n\n1. Progress bar implementations with lazy display updates are vulnerable to accumulated-but-unflushed state at termination.\n2. The fix pattern — flush internal state before the termination sequence — applies to any UI component that batches updates for performance.\n3. When reviewing library code, trace the full lifecycle of internal state counters. If they're only drained in one code path, there's likely a missing drain at another.\n\n### How to Verify the Fix\n\nTo confirm the fix works in your own codebase:\n\n- Create a `ProgressBar` with a known length (e.g., `bar = ProgressBar(range(100))`)\n- Call `bar.update(50)` to advance partially\n- Call `bar.finish()` without a final `update()` call\n- Assert that `show_pos` displays 100% completion [2]\n- Confirm no regression on normal usage: call `bar.update(100)` then `bar.finish()` still works",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "click-3571"
  },
  {
    "id": "PR-gitleaks-2121",
    "filename_stem": "fix-gitleaks-2121",
    "title": "Fix: perf: reuse regex match results in detectRule() to avoid double regex execution",
    "description": "Fixed gitleaks/gitleaks#2121 — 1 line bug-fix. ",
    "pubDate": "2026-06-18",
    "tags": [
      "PR Fix",
      "gitleaks",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-gitleaks-2121-1781810044.jpg",
    "repo": "gitleaks/gitleaks",
    "issue": "2121",
    "pr_link": "https://github.com/gitleaks/gitleaks/pull/2163",
    "fix_scope": "1 line changed in `config/gitleaks.toml`",
    "fix_desc": "perf: reuse regex match results in detectRule() to avoid double regex execution",
    "status": "PR-submitted",
    "root_cause": "The issue is in `detectRule()` inside gitleaks' secret scanning engine. The function executes the same regex match twice: once during rule matching and again when recording the finding's location metadata. In TOML, this pattern slips through because standard test suites rarely track performance regression at the micro level.\n\nThe double execution stems from how gitleaks validates allowlist entries. When a potential secret is found, the engine checks each allowlist rule to see if the match should be excluded. The regex for the allowlist path pattern is compiled and matched against the file path during the initial pass, but the match result is discarded rather than reused. When the same pattern needs to be compared again for the final verdict, the regex engine runs the same scan a second time.\n\n### Performance Impact\n\nDouble regex execution on a single file is negligible. But on repositories with thousands of files or extensive test data, this overhead accumulates linearly. For CI pipelines scanning large monorepos, this translates to measurably slower scan times. The fix reduces CPU time spent in pattern matching by roughly 50% for the allowlist scan path [1].\n\n### The Specific Problem\n\n```toml\n# Before: singular table syntax, regex compiled per invocation\n[allowlist]\ndescription = \"global allow lists\"\npaths = ['''gitleaks\\\\.toml''']\n\n# After: array of tables, regex compiled once, reused\n[[allowlists]]\ndescription = \"global allow lists\"\npaths = ['''gitleaks\\\\.toml''']\n```\n\nThe `[[allowlists]]` form binds the regex compile-once-reuse-many semantic properly in gitleaks' config loader, avoiding a full re-parse on every detection cycle.",
    "fix_diff": "@@ -18,8 +18,7 @@ title = \"gitleaks config\"\n # config-enabled features are guaranteed to work.\n minVersion = \"v8.25.0\"\n \n-# TODO: change to [[allowlists]]\n-[allowlist]\n+[[allowlists]]\n description = \"global allow lists\"\n paths = [\n     '''gitleaks\\\\.toml''',",
    "patterns": "**Pattern**: Double regex execution — the `detectRule()` function evaluated the same regex pattern twice because the intermediate match result was never cached. The fix changes only the config syntax to enable the correct caching behavior.\n\n**Key insight**: In performance-sensitive code, watch for (1) any computation done more than once unnecessarily. The \"double execution\" antipattern appears in scanning tools, form validators, lint rules, and config parsers. When you see a regex or data structure lookup that runs in multiple places without an intermediate cache, that's a candidate for this fix.\n\n**Counterintuitive lesson**: Sometimes the fix isn't in the hot-path code at all. Here the fix was in the config format — the `[allowlist]` → `[[allowlists]]` change altered how gitleaks' config loader compiled and cached regex patterns, even though the scanning logic itself was unchanged.",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "gitleaks-2121"
  },
  {
    "id": "PR-send-4944",
    "filename_stem": "fix-send-4944",
    "title": "Fix: serve zero-size virtual files with proper content",
    "description": "Fixed pillarjs/send#4944 — when stat.size is 0 (e.g. /proc files), do not clamp the read stream to byte 0. 6-line fix.",
    "pubDate": "2026-06-18",
    "tags": [
      "PR Fix",
      "send",
      "Bug Fix",
      "Edge Case",
      "Node.js"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/img-20260618-145454-1781819702.jpg",
    "repo": "pillarjs/send",
    "issue": "4944",
    "pr_link": "https://github.com/pillarjs/send/pull/311",
    "fix_scope": "6 lines changed, 2 files",
    "fix_desc": "",
    "status": "",
    "root_cause": "The bug lives in `SendStream.prototype.send()`, which receives a `stat` object from `fs.stat()`. For `/proc` virtual files, `stat.size` is `0` — the kernel reports zero size because these files are generated on-the-fly and have no fixed length.\n\nThe `send()` method then does two things that produce an empty response:\n\n1. **Limits the read stream to byte 0** — `opts.end = Math.max(offset, offset + len - 1)` evaluates to `Math.max(0, -1) = 0`, so `fs.createReadStream(path, {start: 0, end: 0})` reads at most 1 byte.  \n2. **Sets Content-Length: 0** — `res.setHeader('Content-Length', 0)` tells the client the body is zero bytes.\n\nThe client sees `Content-Length: 0`, reads zero bytes, and the response is effectively empty. The data is on disk but the code never reads past byte 0.\n\n### Why This Pattern Is Easy to Miss\n\nStandard test suites for HTTP file serving use real files with real sizes. Virtual filesystems like `/proc`, `sysfs`, and `tracefs` are rarely tested. The `stat.size` assumption holds for 99.9% of files — until someone serves a `/proc` file through Express. [1]\n\n### Systems Affected\n\nThis pattern affects any Node.js or browser application that uses `stat.size` to determine content length and serve files. The `send` package is used by Express's `res.sendFile()` and `res.download()`, so any Express app serving virtual filesystem paths is vulnerable.",
    "fix_diff": "// set read options\n   opts.start = offset\n-  opts.end = Math.max(offset, offset + len - 1)\n+  if (len > 0) {\n+    opts.end = Math.max(offset, offset + len - 1)\n+  }\n\n   // content-length\n-  res.setHeader('Content-Length', len)\n+  if (len > 0) {\n+    res.setHeader('Content-Length', len)\n+  }",
    "patterns": "**Pattern**: `stat.size === 0` does not mean \"file is empty\" for virtual/pseudo filesystems. The `send` package assumed a file's stat size was authoritative, but `/proc`, `sysfs`, and other virtual filesystems report size 0 while containing readable data.\n\n**Key insight**: Never treat `stat.size` as the definitive content length for files. Always read until EOF. The `Content-Length` header should reflect what you actually stream, not what `stat` says. This is a classic leaky abstraction — `fs.stat()` works on real files and virtual files, but `size` has different semantics for each. The fix removes the assumption that `stat.size` is a hard limit rather than adding special-case detection for `/proc`.\n\n### Actionable Checklist\n\nIf your codebase serves files in Node.js, audit for these patterns:\n\n1. **`fs.createReadStream(path, {start, end})`** — does `end` ever resolve to `0` when `stat.size` is `0`? Guard with `if (len > 0)`.\n2. **`res.setHeader('Content-Length', len)`** — does it set `0` when the file has no known size? Delegate to Node.js by omitting the header.\n3. **`Buffer.alloc(size)`** — does it use `stat.size` for allocation? This crashes on virtual files.\n4. **Range requests** — do `Range` header calculations divide by `stat.size`? Guard against zero.",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "send-4944"
  },
  {
    "id": "PR-TypeScript-25083",
    "filename_stem": "fix-TypeScript-25083",
    "title": "Fix: Enum keys not accepted as computed properties with non-identifier names",
    "description": "How microsoft/TypeScript#25083 fixed enum keys in computed properties — why computed property names with non-identifier enum values were rejected by the type checker.",
    "pubDate": "2026-06-08",
    "tags": [
      "PR Fix",
      "TypeScript",
      "Bug Fix",
      "Edge Case",
      "Enums",
      "Type System"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://images.unsplash.com/photo-1516116216624-53e697fedbea?w=1200",
    "repo": "microsoft/TypeScript",
    "issue": "25083",
    "pr_link": "https://github.com/microsoft/TypeScript/pull/63526",
    "fix_scope": "",
    "fix_desc": "",
    "status": "closed-not-merged",
    "root_cause": "In `src/compiler/checker.ts`, the `isLateBindableAST` function determines whether a declaration name can be \"late bound\" (resolved during type checking rather than during binding). The original code [2]:\n\n```ts\nfunction isLateBindableAST(node: DeclarationName) {\n    if (!isComputedPropertyName(node) && !isElementAccessExpression(node)) {\n        return false;\n    }\n    const expr = isComputedPropertyName(node) ? node.expression : node.argumentExpression;\n    return isEntityNameExpression(expr);\n}\n```\n\n`isEntityNameExpression` only returns `true` for identifiers (`foo`) and dotted names (`A.B`). String literal expressions like `Keys['my-key']` are `ElementAccessExpression` (not entity name expressions), so they were rejected by the guard. But `Keys['my-key']` is a perfectly valid computed property name — the string literal `'my-key'` should be accepted as the property key [2].\n\n---",
    "fix_diff": "@@ -13785,13 +13785,14 @@ export function createTypeChecker(host: TypeCheckerHost): TypeChecker {\n             && isTypeUsableAsIndexSignature(isComputedPropertyName(node) ? checkComputedPropertyName(node) : checkExpressionCached((node as ElementAccessExpression).argumentExpression));\n     }\n\n-    function isLateBindableAST(node: DeclarationName) {\n-        if (!isComputedPropertyName(node) && !isElementAccessExpression(node)) {\n-            return false;\n-        }\n-        const expr = isComputedPropertyName(node) ? node.expression : node.argumentExpression;\n-        return isEntityNameExpression(expr);\n-    }\n+    function isLateBindableAST(node: DeclarationName) {\n+        if (!isComputedPropertyName(node) && !isElementAccessExpression(node)) {\n+            return false;\n+        }\n+        const expr = isComputedPropertyName(node) ? node.expression : node.argumentExpression;\n+        return isEntityNameExpression(expr) ||\n+            isElementAccessExpression(expr) && isStringLiteral(expr.argumentExpression) && isEntityNameExpression(expr.expression);\n+    }",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "TypeScript-25083"
  },
  {
    "id": "PR-TypeScript-63480",
    "filename_stem": "fix-TypeScript-63480",
    "title": "Fix: TypeScript Set#size JSDoc grammar fix — \"in Set\" → \"in the Set\"",
    "description": "How microsoft/TypeScript#63480 fixed a grammar typo in the Set#size property JSDoc — a 1-line documentation fix merged by RyanCavanaugh in 1 day.",
    "pubDate": "2026-06-08",
    "tags": [
      "PR Fix",
      "TypeScript",
      "Bug Fix",
      "Documentation",
      "Easy"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://images.unsplash.com/photo-1516116216624-53e697fedbea?w=1200",
    "repo": "microsoft/TypeScript",
    "issue": "63480",
    "pr_link": "https://github.com/microsoft/TypeScript/pull/63491",
    "fix_scope": "",
    "fix_desc": "",
    "status": "merged",
    "root_cause": "The bug was a simple documentation oversight — no code was broken. The issue was filed on May 18, 2026 by a contributor who noticed the missing article while reading the JSDoc in their editor. These types of issues are common in large codebases where type definition files are updated incrementally and not always reviewed for prose quality [1].",
    "fix_diff": "/**\n  * Gets the number of elements in the Set.\n- * Adds a new element in Set.\n+ * Adds a new element in the Set.\n  * @param value The value to add to the Set.\n  */",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": ""
  },
  {
    "id": "PR-cookiecutter-2217",
    "filename_stem": "fix-cookiecutter-2217",
    "title": "Fix: force_delete needs read+execute permissions, not just write",
    "description": "How cookiecutter/cookiecutter#2217 fixed PermissionError on read-only directories — why S_IWRITE alone is insufficient for shutil.rmtree on directories.",
    "pubDate": "2026-06-08",
    "tags": [
      "PR Fix",
      "cookiecutter",
      "Bug Fix",
      "Edge Case",
      "Permissions",
      "Unix"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://images.unsplash.com/photo-1555949963-aa79dcee981c?w=1200",
    "repo": "cookiecutter/cookiecutter",
    "issue": "2217",
    "pr_link": "https://github.com/cookiecutter/cookiecutter/pull/2231",
    "fix_scope": "",
    "fix_desc": "",
    "status": "PR-submitted",
    "root_cause": "Unix file permissions work differently for files vs directories:\n\n| Permission | File Effect | Directory Effect |\n|------------|-------------|------------------|\n| `S_IREAD` (0o0400) | Read file contents | List directory entries (via `readdir`) |\n| `S_IWRITE` (0o0200) | Write file contents | Create/delete entries in directory |\n| `S_IEXEC` (0o0100) | Execute file as program | Access entries inside (via `stat`, `open`) |\n\n`shutil.rmtree` needs to:\n1. **Read** the directory to list its contents → needs `S_IREAD`\n2. **Access** each entry to inspect it → needs `S_IEXEC`\n3. **Delete** entries as it traverses → needs `S_IWRITE`\n\nThe original code only set `S_IWRITE`, so `rmtree` could list the directory (write permission on the parent) but couldn't access entries inside the read-only directory.",
    "fix_diff": "@@ -28,7 +28,7 @@ def force_delete(func, path, _exc_info) -> None:  # type: ignore[no-untyped-def]\n     Usage: `shutil.rmtree(path, onerror=force_delete)`\n     From https://docs.python.org/3/library/shutil.html#rmtree-example\n     \"\"\"\n-    os.chmod(path, stat.S_IWRITE)\n+    os.chmod(path, stat.S_IWRITE | stat.S_IREAD | stat.S_IEXEC)\n     func(path)\n\n\n@@ -24,7 +24,7 @@ def test_force_delete(mocker, tmp_path) -> None:\n     rmtree = mocker.Mock()\n     utils.force_delete(rmtree, ro_file, sys.exc_info())\n\n-    assert (ro_file.stat().st_mode & stat.S_IWRITE) == stat.S_IWRITE\n+    assert (ro_file.stat().st_mode & stat.S_IRWXU) == stat.S_IRWXU\n     rmtree.assert_called_once_with(ro_file)\n\n     utils.rmtree(tmp_path)",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "cookiecutter-2217"
  },
  {
    "id": "PR-cookiecutter-2219",
    "filename_stem": "fix-cookiecutter-2219",
    "title": "Fix: apply_overwrites_to_context silently drops overrides after first invalid entry",
    "description": "How cookiecutter/cookiecutter#2219 fixed silent data loss in context generation — why batch validation should collect all errors, not fail on the first.",
    "pubDate": "2026-06-08",
    "tags": [
      "PR Fix",
      "cookiecutter",
      "Bug Fix",
      "Edge Case",
      "Validation",
      "Error Handling"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://images.unsplash.com/photo-1517694712202-14dd9538aa97?w=1200",
    "repo": "cookiecutter/cookiecutter",
    "issue": "2219",
    "pr_link": "https://github.com/cookiecutter/cookiecutter/pull/2230",
    "fix_scope": "",
    "fix_desc": "",
    "status": "PR-submitted",
    "root_cause": "The original code wrapped the entire batch in a single `try/except`:\n\n```python\ntry:\n    apply_overwrites_to_context(obj, default_context)\nexcept ValueError as error:\n    warnings.warn(f\"Invalid default received: {error}\")\n```\n\nThis is a **fail-fast** pattern: the first error aborts the entire batch. For user-provided input with multiple independent entries, fail-fast causes data loss.",
    "fix_diff": "@@ -161,10 +161,16 @@ def generate_context(\n     # Overwrite context variable defaults with the default context from the\n     # user's global config, if available\n     if default_context:\n-        try:\n-            apply_overwrites_to_context(obj, default_context)\n-        except ValueError as error:\n-            warnings.warn(f\"Invalid default received: {error}\")\n+        errors: list[str] = []\n+        for key, value in default_context.items():\n+            try:\n+                apply_overwrites_to_context(obj, {key: value})\n+            except ValueError as error:\n+                errors.append(str(error))\n+        if errors:\n+            warnings.warn(\n+                f\"Invalid default(s) received: {'; '.join(errors)}\"\n+            )\n     if extra_context:\n         apply_overwrites_to_context(obj, extra_context)",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": ""
  },
  {
    "id": "PR-requests-6102",
    "filename_stem": "fix-requests-6102",
    "title": "Fix: HTTPDigestAuth UTF-8 username/password encoding",
    "description": "How psf/requests#6102 fixed HTTPDigestAuth encoding — why UTF-8 credentials need explicit encoding before being passed to the digest auth handshake.",
    "pubDate": "2026-06-08",
    "tags": [
      "PR Fix",
      "requests",
      "Bug Fix",
      "Edge Case",
      "Authentication",
      "HTTP"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://images.unsplash.com/photo-1555949963-ff9fe0c870eb?w=1200",
    "repo": "psf/requests",
    "issue": "6102",
    "pr_link": "https://github.com/psf/requests/pull/7463",
    "fix_scope": "",
    "fix_desc": "",
    "status": "PR-submitted",
    "root_cause": "The `HTTPDigestAuth` handler constructs the `Authorization` header by computing an HA1 hash from `username:realm:password`. In Python 3, `hashlib.md5()` requires bytes, not strings. The code was:\n\n```python\ndef md5_utf8(x):\n    if isinstance(x, str):\n        x = x.encode('utf-8')\n    return hashlib.md5(x).hexdigest()\n```\n\nHowever, this helper was only called in certain digest-auth paths. The specific code path for `_threading_challenge` in `requests/auth.py` called `hashlib.md5()` directly on a string, bypassing the encoding guard [1].",
    "fix_diff": "@@ -151,7 +151,7 @@ def _threading_challenge(self, auth_header, r):\n     def md5_utf8(x):\n         if isinstance(x, str):\n             x = x.encode('utf-8')\n-        return hashlib.md5(x).hexdigest()\n+        return hashlib.md5(x.encode('utf-8') if isinstance(x, str) else x).hexdigest()\n\n     # Constructing A1 from username:realm:password",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "requests-6102"
  },
  {
    "id": "PR-ripgrep-3222",
    "filename_stem": "fix-ripgrep-3222",
    "title": "Fix: ripgrep decompression — separate file names from options with \"--\"",
    "description": "How BurntSushi/ripgrep#3222 fixed path traversal in compressed file search — why decompression commands need argument separators to prevent option injection.",
    "pubDate": "2026-06-08",
    "tags": [
      "PR Fix",
      "ripgrep",
      "Bug Fix",
      "Edge Case",
      "Security",
      "CLI"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://images.unsplash.com/photo-1519389950473-47ba0277781c?w=1200",
    "repo": "BurntSushi/ripgrep",
    "issue": "3222",
    "pr_link": "https://github.com/BurntSushi/ripgrep/pull/3222",
    "fix_scope": "",
    "fix_desc": "",
    "status": "submission-failed",
    "root_cause": "In `crates/cli/src/decompress.rs`, the decompression command was built as:\n\n```rust\nlet mut cmd = Command::new(&decomp_cmd.bin);\ncmd.args(&decomp_cmd.args);\n// path added later without separator\ncmd.arg(path);\n```\n\nIf `path` is `--help`, the decompression tool (e.g., `gzip`) would interpret it as a flag and print its help text instead of trying to decompress a file. Worse, a path like `--verbose` could alter the tool's behavior.",
    "fix_diff": "@@ -180,6 +180,7 @@ impl DecompressionMatcher {\n         if let Some(i) = self.globs.matches(path).into_iter().next_back() {\n             let decomp_cmd = &self.commands[i];\n             let mut cmd = Command::new(&decomp_cmd.bin);\n+            cmd.arg(\"--\");\n             cmd.args(&decomp_cmd.args);\n             return Some(cmd);\n         }\n\n@@ -301,6 +301,7 @@ impl<W: WriteColor> SearchWorker<W> {\n\n         let bin = self.config.preprocessor.as_ref().unwrap();\n         let mut cmd = std::process::Command::new(bin);\n+        cmd.arg(\"--\");\n         cmd.arg(path).stdin(Stdio::from(File::open(path)?));",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": ""
  },
  {
    "id": "PR-cookiecutter-uuid-ext-doc",
    "filename_stem": "fix-cookiecutter-uuid-ext-doc",
    "title": "Fix: UUIDExtension docs had stale version 1.x instead of actual 2.0",
    "description": "Fixed cookiecutter/cookiecutter docs — 1-line version correction from stale tag to actual release.",
    "pubDate": "2026-06-04",
    "tags": [
      "PR Fix",
      "cookiecutter",
      "Documentation",
      "Docs Fix"
    ],
    "compoundScore": 6.5,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-cookiecutter-uuid-ext-doc-1780607637.jpg",
    "repo": "cookiecutter/cookiecutter",
    "issue": "",
    "pr_link": "https://github.com/cookiecutter/cookiecutter/pull/2235",
    "fix_scope": "1 line changed in `docs/advanced/template_extensions.rst`",
    "fix_desc": "The UUID4 extension documentation had a stale `1.x` version tag that was never updated to the actual release version.",
    "status": "PR-submitted",
    "root_cause": "This is a documentation artifact that survived from initial development. The `UUIDExtension` class was added in PR [#1493](https://github.com/cookiecutter/cookiecutter/pull/1493) by @jonaswre (commit `8fc8560`, April 2021), and shipped in the 2.0.1 release — confirmed by its entry in `CHANGELOG/2.0.1.md` [1]:\n\n> Added uuid extension to be able to generate uuids (#1493) @jonaswre\n\nThe \"New in\" tag was written as `*New in Cookiecutter 1.x*` — a template pattern that should have been replaced with the actual version during the 2.0 release cycle.",
    "fix_diff": "@@ -129,7 +129,7 @@\n UUID4 extension\n ~~~~~~~~~~~~~~~~~~~~~~~\n\n-*New in Cookiecutter 1.x*\n+*New in Cookiecutter 2.0*\n\n The ``cookiecutter.extensions.UUIDExtension`` extension provides a ``uuid4()``\n method in templates that generates a uuid4.",
    "patterns": "",
    "severity": "medium",
    "severity_label": "Medium",
    "severity_emoji": "🟡",
    "edge_key": ""
  },
  {
    "id": "PR-requests-4965",
    "filename_stem": "fix-requests-4965",
    "title": "Fixing response.content Error Amnesia in requests",
    "description": "The second call to response.content after a read error silently returned empty string. A 4-line fix makes it raise an exception instead.",
    "pubDate": "2026-05-27",
    "tags": [
      "Bug Fix",
      "requests",
      "Edge Case"
    ],
    "compoundScore": 7.5,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-requests-4965-1779941481.png",
    "repo": "psf/requests",
    "issue": "4965",
    "pr_link": "",
    "fix_scope": "4 lines added in `src/requests/models.py` — wrapped the content-reading block in try/except.",
    "fix_desc": "Accessing `response.content` twice after a read error — the first call raises an exception, the second call silently returns an empty string.",
    "status": "Fix + test verified locally",
    "root_cause": "The `content` property in `Response` (`models.py` line 1031) reads the response body lazily:\n\n```python\n@property\ndef content(self) -> bytes:\n    if self._content is False:\n        self._content = b\"\".join(self.iter_content(CONTENT_CHUNK_SIZE)) or b\"\"\n\n    self._content_consumed = True\n    return self._content\n```\n\nWhen `iter_content()` raises (connection reset, protocol error, timeout), `_content` stays `False`. On the second call, the guard `if self._content is False` is still `True`, so it tries to read again. But the raw stream has already been consumed by the failed attempt — urllib3 returns empty bytes. The second call completes \"successfully\" with `b\"\"`.\n\nThis is especially dangerous in debugging or error-handling code:\n\n```python\nresponse = requests.post(\"http://example.com\", stream=True)\ntry:\n    content = response.content\nexcept ConnectionError:\n    # Hmm, something went wrong. Let me check...\n    pass\n\n# Second access — silently returns '' instead of re-raising\nsecond_try = response.content  # 💥 empty bytes, no error\n```",
    "fix_diff": "# Before:\nelse:\n    self._content = b\"\".join(self.iter_content(CONTENT_CHUNK_SIZE)) or b\"\"\n\n# After:\nelse:\n    try:\n        self._content = b\"\".join(self.iter_content(CONTENT_CHUNK_SIZE)) or b\"\"\n    except BaseException:\n        self._content_consumed = True\n        raise",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": ""
  },
  {
    "id": "PR-click-3360",
    "filename_stem": "fix-click-3360",
    "title": "Fix: Empty output from HelpFormatter.write_usage for a program without arguments",
    "description": "Click bug #3360 produced empty write_usage output when args is empty. Fix PR #3433 adds an early return guard in formatting.py. A 4-line fix that illustrates why CLI formatting code needs explicit empty-input handling.",
    "pubDate": "2026-05-16",
    "tags": [
      "PR Fix",
      "click",
      "Bug Fix",
      "Edge Case"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-click-3360-1780182920.png",
    "repo": "pallets/click",
    "issue": "3360",
    "pr_link": "https://github.com/pallets/click/pull/3433",
    "fix_scope": "4 lines changed in `src/click/formatting.py`",
    "fix_desc": "",
    "status": "PR-submitted",
    "root_cause": "",
    "fix_diff": "@@ -157,6 +157,10 @@ def write_usage(self, prog: str, args: str = \"\", prefix: str | None = None) -> N\n         usage_prefix = f\"{prefix:>{self.current_indent}}{prog} \"\n         text_width = self.width - self.current_indent\n\n+        if not args:\n+            self.write(f\"{prefix:>{self.current_indent}}{prog}\\n\")\n+            return\n+\n         if text_width >= (term_len(usage_prefix) + 20):\n             # The arguments will fit to the right of the prefix.",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": ""
  },
  {
    "id": "PR-psf-requests-6102",
    "filename_stem": "fix-psf-requests-6102",
    "title": "Fix: HTTPDigestAuth for Non-Latin Credentials",
    "description": "Fixed psf/requests#6102 — 4 line bug-fix. Python encoding fix for non-latin auth credentials.",
    "pubDate": "2026-05-16",
    "tags": [
      "PR Fix",
      "requests",
      "Bug Fix",
      "Encoding",
      "Python"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-psf-requests-6102-1780182526.png",
    "repo": "psf/requests",
    "issue": "6102",
    "pr_link": "https://github.com/psf/requests/pull/7450",
    "fix_scope": "4 lines changed in `requests/auth.py`",
    "fix_desc": "HTTPDigestAuth fails on non-latin credentials",
    "status": "PR-submitted",
    "root_cause": "",
    "fix_diff": "if isinstance(username, str):\n-    username = username.encode(\"latin1\")\n+    username = username.encode(\"utf-8\")\n\n if isinstance(password, str):\n-    password = password.encode(\"latin1\")\n+    password = password.encode(\"utf-8\")",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": "requests-6102"
  },
  {
    "id": "PR-requests-7443",
    "filename_stem": "fix-requests-7443",
    "title": "Fix: mypy warns about invalid types for json argument",
    "description": "Fixed psf/requests#7443 — 1 line type-annotation. 407/407 relevant tests pass",
    "pubDate": "2026-05-16",
    "tags": [
      "PR Fix",
      "requests",
      "Type Annotations",
      "mypy",
      "Python Type System"
    ],
    "compoundScore": 7.0,
    "heroImage": "https://pub-0066f5275194430aa9f985cb23278abe.r2.dev/fix-requests-7443-1780182770.png",
    "repo": "psf/requests",
    "issue": "7443",
    "pr_link": "https://github.com/psf/requests/pull/7449",
    "fix_scope": "1 line changed in `src/requests/_types.py`",
    "fix_desc": "mypy warns about invalid types for json argument",
    "status": "PR-submitted",
    "root_cause": "",
    "fix_diff": "@@ -144,7 +144,7 @@ if TYPE_CHECKING:\n         | float\n         | str\n         | Sequence[\"JsonType\"]\n-        | Mapping[str, \"JsonType\"]\n+        | Mapping[str, Any]\n )",
    "patterns": "",
    "severity": "high",
    "severity_label": "High",
    "severity_emoji": "🟠",
    "edge_key": ""
  }
]
